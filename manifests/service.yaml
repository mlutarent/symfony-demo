apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: symfony-demo
  name: symfony-demo
spec:
  ports:
    - name: "9090"
      port: 9090
      targetPort: 80
  selector:
    io.kompose.service: symfony-demo
